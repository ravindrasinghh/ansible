---

- hosts : test-server

  tasks :
   - name : install nginx
     become : true
     become_user : test
     gather_facts : true
     yum  : pkg=nginx state=installed

     notify:
        - start nginx

  handlers :
   - name : start nginx
     service : name=nginx state=started
~                                                                               
~                                         
